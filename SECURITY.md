# Security Policy

Report vulnerabilities through GitHub private vulnerability reporting. Do not
put credentials, private records, or reproduction data from a real organization
in a public issue.

The public demo is limited to synthetic fixture data. Production deployment
requires a real identity provider, encrypted persistent storage, explicit CORS,
rate limiting, retention enforcement, and centralized redacted logs.
